using System;

namespace BullyBot
{
    public class AlarmService
    {
        private SchedulerService scheduler;

        public AlarmService(SchedulerService scheduler)
        {
            this.scheduler = scheduler;
        }

        private void ScheduleAlarms()
        {

        }
    }
}